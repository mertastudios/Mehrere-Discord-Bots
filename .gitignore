# Abhängigkeiten
node_modules/

# Umgebungsvariablen – NIE einchecken! (Token sind Geheimnisse!)
.env
.env.local
.env.*.local

# Bot-lokale Daten (RAM-Fallback, wird bei Turso nicht gebraucht)
bots/xp-level-bot/xp-data.json
data/
*.db
*.sqlite
*.libsql

# Logs
*.log
logs/

# Betriebssystem-Kram
.DS_Store
Thumbs.db

# Bot-lokale Daten (RAM-Fallback, wird bei Turso nicht gebraucht)
bots/security-bot/security-data.json
bots/security-bot/security-gemini-data.json
